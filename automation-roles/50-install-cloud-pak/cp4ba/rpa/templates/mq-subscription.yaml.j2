apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ibm-mq-subscription
  namespace: "{{ rpa_project_name }}"
spec:
  channel: "{{ rpa_mq_operator_channel }}"
  name: ibm-mq
  source: ibmmq-operator-catalogsource
  sourceNamespace: "{{ rpa_project_name }}"
