- name: Remove usage Config Map
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: ConfigMap
    name: "000-{{ _current_cp4ba_cluster.project }}-usage"
    namespace: "{{ _current_cp4ba_cluster.project }}"
    force: false
    wait: true
    wait_sleep: 15
    wait_timeout: 15
