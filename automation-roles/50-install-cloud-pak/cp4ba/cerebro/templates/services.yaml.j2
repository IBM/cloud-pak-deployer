kind: Service
apiVersion: v1
metadata:
  name: cerebro
  namespace: "{{ cerebro_project_name }}"
  labels:
    app: cerebro  
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: cerebro
  selector:
    app: cerebro
  type: ClusterIP
  sessionAffinity: None
