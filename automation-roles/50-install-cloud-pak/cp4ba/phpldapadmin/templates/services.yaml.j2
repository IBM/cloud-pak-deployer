kind: Service
apiVersion: v1
metadata:
  name: phpldapadmin
  namespace: "{{ phpldapadmin_project_name }}"
  labels:
    app: phpldapadmin
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
  type: ClusterIP
  selector:
    app: phpldapadmin