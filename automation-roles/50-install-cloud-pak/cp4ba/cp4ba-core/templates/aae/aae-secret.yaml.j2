apiVersion: v1
kind: Secret
metadata:
  name: "{{ cp4ba_cr_meta_name }}-workspace-aae-app-engine-admin-secret"
  namespace: "{{ cp4ba_project_name }}"
type: Opaque
stringData:
  AE_DATABASE_PWD: "{{ cp4ba_postgresql_universal_password }}"
  AE_DATABASE_USER: aaedb
  REDIS_PASSWORD: "{{ cp4ba_universal_password }}"
