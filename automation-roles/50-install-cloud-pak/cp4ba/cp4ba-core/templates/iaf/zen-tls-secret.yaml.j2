kind: Secret
apiVersion: v1
metadata:
  name: cpd-route-tls-secret
  namespace: "{{ cp4ba_project_name }}"
data:
  tls.key: "{{ tls_key }}"
  tls.crt: "{{ tls_crt }}"
  ca.crt: "{{ ca_crt }}"
type: Opaque
