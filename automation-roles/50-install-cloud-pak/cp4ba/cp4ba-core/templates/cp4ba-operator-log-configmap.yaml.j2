apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ cp4ba_project_name }}-opertor-log"
  namespace: "{{ cp4ba_output_namespace }}"
data:
  cp4ba-operator.log: |- 
    {{ cp4ba_failed_tasks_content | indent(4) }}
