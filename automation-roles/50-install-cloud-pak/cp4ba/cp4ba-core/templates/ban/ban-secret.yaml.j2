apiVersion: v1
kind: Secret
metadata:
  name: ibm-ban-secret
  namespace: "{{ cp4ba_project_name }}"
type: Opaque
stringData:
  navigatorDBUsername: icndb
  navigatorDBPassword: "{{ cp4ba_postgresql_universal_password }}"
  keystorePassword: "{{ cp4ba_universal_password }}"
  ltpaPassword: "{{ cp4ba_universal_password }}"
  appLoginUsername: "{{ lc_principal_admin_user }}"
  appLoginPassword: "{{ lc_principal_admin_password }}"
  jMailUsername: "{{ lc_principal_admin_user }}@{{ lc_ldap_domain }}"
  jMailPassword: "{{ cp4ba_universal_password }}"
