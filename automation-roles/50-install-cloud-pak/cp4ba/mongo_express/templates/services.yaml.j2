kind: Service
apiVersion: v1
metadata:
  name: mongo-express
  namespace: "{{ mongo_express_project_name }}"
  labels:
    app: mongo-express
spec:
  selector:
    app: mongo-express
  ports:
    - name: ui
      port: 8081
      targetPort: ui
