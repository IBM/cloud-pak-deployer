- name: Remove Service
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: Service
    namespace: "{{ openldap_project_name }}"
    name: openldap
    wait: true
    wait_sleep: 15
    wait_timeout: 120

- name: Remove Deployment
  kubernetes.core.k8s:
    state: absent
    api_version: apps/v1
    kind: Deployment
    namespace: "{{ openldap_project_name }}"
    name: openldap
    wait: true
    wait_sleep: 15
    wait_timeout: 120

- name: Remove ConfigMap env
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: ConfigMap
    namespace: "{{ openldap_project_name }}"
    name: openldap-env
    wait: true
    wait_sleep: 15
    wait_timeout: 120

- name: Remove ConfigMap LDIF
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: ConfigMap
    namespace: "{{ openldap_project_name }}"
    name: openldap-customldif
    wait: true
    wait_sleep: 15
    wait_timeout: 120

- name: Remove Secret
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: Secret
    namespace: "{{ openldap_project_name }}"
    name: openldap
    wait: true
    wait_sleep: 15
    wait_timeout: 120

- name: Remove PersistentVolumeClaim
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: PersistentVolumeClaim
    namespace: "{{ openldap_project_name }}"
    name: openldap-data
    wait: true
    wait_sleep: 15
    wait_timeout: 120
