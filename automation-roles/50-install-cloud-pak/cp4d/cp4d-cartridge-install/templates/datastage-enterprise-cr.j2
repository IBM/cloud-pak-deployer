apiVersion: dfd.cpd.ibm.com/v1alpha1
kind: DataStageService
metadata:
  name: datastage     # This is the recommended name, but you can change it
  namespace: {{ current_cp4d_cluster.project }}     # Replace with the project where you will install DataStage
spec:
  license:
    accept: true
    license: Enterprise
  version: {{ current_cp4d_cartridge.version }}
  storageClass: {{ ocp_storage_class_file }}
