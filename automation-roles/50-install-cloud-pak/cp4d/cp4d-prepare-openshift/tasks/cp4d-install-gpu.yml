---
- name: Install Node Feature Discovery and GPU operators if installation has been set to auto
  include_role:
    name: openshift-gpu
  vars:
    _p_openshift_cluster: "{{ _current_openshift_cluster }}"