---
- name: Delete all OpenPages instances
  include_role:
    name: cp4d-openpages-instance
  vars:
    _p_delete_all_instances: True
  when: (_current_cartridge_cr.olm_utils_name | default("")) == "openpages"