---
- name: Create KubeletConfig
  include_tasks: create-kubelet-config.yml
  when: cloud_platform != 'ibm-cloud'

- name: Apply KubeletConfig for IBM Cloud ROKS
  include_tasks: ibmcloud-apply-kubelet-config.yml
  when: cloud_platform == 'ibm-cloud'