{
    "auths": {
{% if (_source_registry_auth | default('')) != '' %}
        "{{ _source_registry_url }}": {
            "auth": "{{ _source_registry_auth | b64encode }}"
        }
{% endif %}
{% if (_source_registry_auth | default('')) != '' and (_target_registry_auth | default('')) != '' %}
        ,
{% endif %}
{% if (_target_registry_auth | default('')) != '' %}
        "{{ _target_registry_url }}": {
            "auth": "{{ _target_registry_auth | b64encode }}"
        }  
{% endif %}
    }
}