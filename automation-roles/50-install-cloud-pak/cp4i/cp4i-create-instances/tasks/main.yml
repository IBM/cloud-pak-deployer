---

- name: Create each of the defined capability or rutime instances
  include_tasks: cp4i-create-instance.yml
  loop: "{{ current_cp4i_cluster.instances | default([]) }}"
  loop_control:
    loop_var: current_cp4i_instance
