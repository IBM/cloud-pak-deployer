---
- name: Create ImageContentSourcePolicy
  include_tasks: create-icsp.yml
  when: cloud_platform != 'ibm-cloud'

- name: Apply ImageContentSourcePolicy settings for IBM Cloud ROKS
  include_tasks: ibmcloud-apply-icsp.yml
  when: cloud_platform == 'ibm-cloud'