{
    "auths": {
        "{{ entitled_registry }}": {
            "auth": "{{ entitled_auth | b64encode }}"
        },
        "{{ ibm_cloud_cr_server }}": {
            "auth": "{{ icr_auth | b64encode }}"
        }  
    }
}