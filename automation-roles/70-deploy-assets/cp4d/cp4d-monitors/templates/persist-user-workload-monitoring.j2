apiVersion: v1
kind: ConfigMap
metadata:
  name: user-workload-monitoring-config
  namespace: openshift-user-workload-monitoring
data:
  config.yaml: |
    prometheus:
      volumeClaimTemplate:
        spec:
          storageClassName: {{ _monitor_persist_sc }}
          volumeMode: Filesystem
          resources:
            requests:
              storage: {{ _monitor_persist_size_gb }}Gi