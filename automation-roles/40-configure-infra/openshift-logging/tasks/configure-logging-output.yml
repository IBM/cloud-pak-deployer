---
- debug:
    msg: "{{ lookup('template','openshift-clusterlogforwarder-patch.j2') }}"

- name: Patch ClusterLogForwarder
  shell: |
    oc patch -n openshift-logging clusterlogforwarder/instance --type=merge \
      --patch {{ lookup('template','openshift-clusterlogforwarder-patch.j2') }}