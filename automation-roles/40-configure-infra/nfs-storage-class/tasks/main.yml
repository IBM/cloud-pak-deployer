---
# tasks file for nfs_storage

- include: ibmcloud-configure-nfs-storage-class.yml
  when: cloud_platform == 'ibm-cloud'

- include: existing-ocp-configure-nfs-storage-class.yml
  when: cloud_platform == 'existing-ocp'
