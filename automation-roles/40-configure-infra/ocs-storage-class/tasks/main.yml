---

# TODO: Implemnent OCS storage class provisioning
- debug:
    msg: "PLACEHOLDER for OCS storage class"
  when: cloud_platform == 'ibm-cloud'

- name: Configure OCS storage class for IBM Cloud VPC
  include_tasks: ibmcloud-configure-ocs.yml
  when: cloud_platform == 'ibm-cloud'