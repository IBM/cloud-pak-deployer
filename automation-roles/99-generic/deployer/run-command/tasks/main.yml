---
- include_role:
    name: log-deployer-activity
  vars:
    _p_activity_description: "{{ _p_activity_description }}"
    _p_activity_command: "{{ _p_activity_command }}"

- name: "Run command: {{ _p_activity_command }}"
  shell: |
    {{ _p_activity_command }}
  when: not cpd_dry_run