---
cp4d:
- project: acme_uat             # Mandatory
  openshift_cluster_name: acme_dev_uat   # Mandatory, references the ROKS cluster that was defined
  container_registry_namespace: roks-cpd-354  # Mandatory, references the ibm container registry namespace definition
  cartridges:
  - cartridge: lite             # Mandatory, indicates the cartridge to be installed
    version: 3.5.0.0            # Optional, taken from defaults if not specified
    size: medium                # Optional, taken from defaults if not specified
  - cartridge: wsl
    version: 3.5.3.0            # Optional, taken from defaults if not specified
    size: small                 # Optional, taken from defaults if not specified
#  - cartridge: dv
#    version: 3.5.2.0
#    instance_size: x-large
#  - cartridge: ca
#    version: 3.5.4.0
#    instance_size: small
#    metastore_ref: ca_meta_db2
#  - cartridge: db2-oltp
#    version: 3.5.1.0
#    instances:
#    - name: ca_meta_db2
#      size: small
#    - name: ods
#      size: medium
#- project: acme_dev
#  openshift_name: acme_dev_uat  # Mandatory, references the ROKS cluster that was defined
#  cartridges:
#  - cartridge: lite             # Mandatory, indicates the cartridge to be installed
#    version: 3.5.1.0            # Optional, taken from defaults if not specified
#    size: small                 # Optional, taken from defaults if not specified
#  - cartridge: wsl              # Mandatory, indicates the cartridge to be installed
#    version: 3.5.4.0            # Optional, taken from defaults if not specified
#    size: small                 # Optional, taken from defaults if not specified