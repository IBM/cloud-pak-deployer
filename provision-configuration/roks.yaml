---
roks:
- name: acme_dev_uat # Mandatory
  kube_version: 4.6_openshift # Mandatory
  # Overrides for the default worker pool
  #worker_flavour: m # Mandatory
  worker_flavour: b3c.16x64
  worker_count: 2
  classic_datacenter: fra04
  resource_group_id: 3454bd31f029474e8e130b686fa3c7f9
  max_worker_count: 10 # Optional, default is no maximum
  # Reference the infrastructure that this will be provisioned on
  infrastructure: # Mandatory
    type: classic # Mandatory, can be vpc or classic
    #vpc_name: acme_client_access_dev_uat # Mandatory if type==vpc
    classic_name: acme_client_access_dev_uat # Mandatory if type==classic
  storage_classes: # Mandatory, at least one element must exist. Elements can be either nfs or ocs
  - nfs 
  #- ocs # If ocs is defined, an additional worker pool will be added to the cluster
  #addons: # All add-ons are optional
  #  worker_pools: # Additional worker pools to be defined for the ROKS cluster
  #  - name: uat_ml_servers
  #    worker_flavour: xxl