---
global_config:
  environment_name: sample
  cloud_platform: existing-ocp
  env_id: pluto-01
  confirm_destroy: False
  optimize_deploy: True

openshift:
- name: "{{ env_id }}"
  ocp_version: detect
  cluster_name: "{{ env_id }}"
  domain_name: example.com
  mcg:
    install: False
    storage_type: storage-class
    storage_class: not-specified
  gpu:
    install: False
  openshift_storage:
  - storage_name: netapp-trident
    storage_type: custom
    ocp_storage_class_file: ontap-nas
    ocp_storage_class_block: ontap-san