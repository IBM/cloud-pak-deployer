---
openshift:
- name: {{ env_id }}
  ocp_version: 4.7
  cluster_name: {{ env_id }}
  domain_name: ocp47.tec.uk.ibm.com
  openshift_storage:
  - storage_name: nfs-storage
    storage_type: nfs
    ocp_storage_class_file: managed-nfs-storage
# Optional parameters if you want to override the storage class used
    # ocp_storage_class_file: nfs-client 
    # ocp_storage_class_block: nfs-client
